---
- hosts: all
  vars_files:
    - ../default.config.yml
  tasks:
    - block:
      - set_fact:
          interactive_mode: false
      - import_tasks: test-rdiff.yml
      rescue:
        - import_tasks: test-cleanup.yml
